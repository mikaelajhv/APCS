import info.gridworld.actor.Bug;

public class DancingBug extends Bug {

    private int[] d;
   
    public DancingBug(int[] dance)
    {
    	d = dance;
    }

    public void act()
    {
    	//for loop for turns in the array, index++ 
    	if(d.length > 
    	{
    		
    	}
    	
    	
    	/*
        if (steps < sideLength && canMove())
        {
            move();
            steps++;
        }
        else
        {
            turn();
            turn();
            sideLength++;
            steps = 0;
        } */
    }
}
